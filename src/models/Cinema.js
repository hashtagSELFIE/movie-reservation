class Cinema {
  constructor(id, branch, theater_no, location, capacity) {
    this.id = id;
    this.branch = branch;
    this.theater_no = theater_no;
    this.location = location;
    this.capacity = capacity;
  }
}

export default Cinema;
